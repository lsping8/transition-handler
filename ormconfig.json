{
  "type": "postgres",
  "host": "localhost",
  "port": 5433,
  "username": "postgres",
  "password": "pandora",
  "database": "anime",
  "logging": false,
  "synchronize": false,
  "entities": ["src/persistance/*.model.ts"],
  "migrations": ["src/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
