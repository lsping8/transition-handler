{
  "type": "postgres",
  "host": "localhost",
  "port": 5433,
  "username": "postgres",
  "password": "pandora",
  "database": "anime",
  "logging": true,
  "synchronize": true,
  "entities": ["src/resolvers/**/persistance/*.model.ts"],
  "migrations": ["src/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
